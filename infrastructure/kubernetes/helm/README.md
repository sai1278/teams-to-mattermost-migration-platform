# Helm Placeholder

This directory is reserved for a future Helm chart that packages the parser job,
queue workers, and supporting configuration for shared cluster deployments.

For now, use the Kustomize base and overlays under `infrastructure/kubernetes/`.
